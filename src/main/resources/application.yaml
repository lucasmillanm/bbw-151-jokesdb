spring:
  datasource:
    url: jdbc:postgresql://localhost:6000/localdb
    username: root
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate.ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQL95Dialect
    open-in-view: false
